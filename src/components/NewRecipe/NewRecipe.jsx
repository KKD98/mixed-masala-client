import React from 'react';

const NewRecipe = ({recipe}) => {
    const {name, picture, experience, number_of_recipes, recipeName, rating,    } = recipe;
    return (
        <div>
            
        </div>
    );
};

export default NewRecipe;